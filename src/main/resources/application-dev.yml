server:
  port: 8080

spring:
  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb+srv://mosaichostelvaranasi:mosaic2025@cluster0.sxnlz6z.mongodb.net/mosaiccafe}
      auto-index-creation: true

logging:
  level:
    root: INFO
    com.hostel.ordering: DEBUG
    org.springframework.data.mongodb: DEBUG

cors:
  allowed-origins: ${CORS_ALLOWED_ORIGINS:http://192.168.1.50:3000,http://127.0.0.1:3000}